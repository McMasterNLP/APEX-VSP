"""Item 3A validation engine: matching policies + metric families.

@remarks
Deliberately narrow for this pass: exactly one matching policy
(`exact_span_match`) and exactly one metric family (span classification).
See `registry.py` and `metrics.py` for how a second of either would slot in.
"""
