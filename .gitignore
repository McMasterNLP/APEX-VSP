.vscode/
.idea/
.DS_Store

# Local environment (secrets). Keep `.env.example` files committed; never commit real `.env`.
.env

# Local strategy / internal planning docs
ARCHITECTURAL_ALIGNMENT_AND_DEPTH_REVIEW.md
SUBSYSTEM_AUDIT.md
presentation/feb-27-meeting.md
local/
handoff.txt

# Accidental root-level installs (frontend/backend each manage their own)
node_modules/
